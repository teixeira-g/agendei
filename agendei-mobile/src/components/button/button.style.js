import {COLORS, FONT_SIZE} from '../../constants/theme'

export const styles = {
    btn:{
        width:"100%",
        backgroundColor: COLORS.blue,
        padding: 12,
        borderRadius: 6,
    },

    text: {
        color: "#fff",
        fontSize: FONT_SIZE.md,
        textAlign: "center",
    }
}